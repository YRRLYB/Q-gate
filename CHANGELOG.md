# Changelog

## v1.1.0 - 2026-03-31

- 新增后台独立的“绑定记录”视图，可直接查看已绑定身份卡片、分数、验证码状态和提交时间。
- 新增 QQ 头像本地缓存，绑定卡片会优先读取服务端本地头像资源。
- 新增后台手动删除绑定记录能力，删除后会同时清理头像缓存，并恢复该身份的再次答题资格。
- 新增跨设备续答支持，未完成的答题可通过原答题链接在另一台设备上恢复。
- 收紧身份防刷逻辑：同一 QQ 或同一 Minecraft 账号名命中已绑定、冷却或时间窗限制时都会被拦截。
- 收紧未完成会话逻辑：只有 QQ 和角色名都一致时才允许恢复原会话，避免误续到他人的进行中记录。
- 收紧后台头像访问权限，绑定头像改为仅管理员工作台可读。
- 管理端删除确认改为站内悬浮确认卡，不再使用浏览器原生弹窗。
- 强化运行配置安全校验：弱管理员密码、占位 TOKEN_SECRET 会在 API 启动时直接拒绝运行。
- 项目版本从 v1.0 升级到 v1.1，站内部署信息同步显示 `v1.1`。

## v1.0.0

- 发布首个可用版本，包含 React + Vite 前端、Fastify API、YAML 题库和站点配置。
- 提供基础答题流程、验证码校验接口、管理工作台与 SQLite 运行态存储。
